---
import { getHrefLangTags } from "../utils/routeManager";

const hrefLangTags = getHrefLangTags(Astro.url);
---

{
    hrefLangTags.map(({ lang, href }) => (
        <link rel="alternate" hreflang={lang} href={href} />
    ))
}
