---
const navHeight = Astro.props.navHeight ?? "0vh";
---

<section class="flex justify-center items-center">
    <slot />
</section>
<style define:vars={{ navHeight }}>
    section {
        height: 100%;
        min-height: calc(100vh - var(--navHeight));
        width: 100%;
    }
</style>
